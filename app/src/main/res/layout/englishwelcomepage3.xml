<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/englishbg">

    <!-- Toolbar -->
    <include layout="@layout/toolbar" />

    <!-- Centered English icon -->
    <ImageView
        android:layout_width="337dp"
        android:layout_height="282dp"
        android:layout_gravity="center"
        android:src="@drawable/englishicon" />

    <!-- "Let's Start the Quiz!" text below icon -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="190dp"
        android:gravity="center"
        android:text="Let's Start the Quiz!"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Start Quiz button at bottom center -->
    <Button
        android:id="@+id/eng_startbtn"
        android:layout_width="158dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="100dp"
        android:background="@drawable/btn_gradientorange"
        android:text="Start Quiz"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/white" />

    <!-- Large "English" text at top center -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|top"
        android:layout_marginTop="100dp"
        android:clickable="true"
        android:gravity="center"
        android:text="English"
        android:textColor="@color/black"
        android:textSize="80dp"
        android:textStyle="bold" />

</FrameLayout>
